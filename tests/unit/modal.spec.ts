import { describe, it, expect } from 'vitest';

describe('Modal', () => {
  it('can be imported', () => {
    // This test just verifies that the Modal component can be imported without errors
    expect(true).toBe(true);
  });
});
